import { Advantage } from "../../types/Advantage";


interface TradeAdvantageModalProps {
  advantage: Advantage; 
  onClose: () => void;
}

export const TradeAdvantageModal: React.FC<TradeAdvantageModalProps> = ({ advantage, onClose }) => {
    return(
        <></>
    )
}